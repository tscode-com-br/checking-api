# DigitalOcean production environment
APP_ENV=production
APP_HOST=0.0.0.0
APP_PORT=8000
APP_WORKERS=1
APP_KEEPALIVE_SECONDS=5
APP_TIMEOUT_SECONDS=90
APP_GRACEFUL_TIMEOUT_SECONDS=30
APP_MAX_REQUESTS=1000
APP_MAX_REQUESTS_JITTER=100

POSTGRES_DB=checking
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_BIND_ADDRESS=127.0.0.1
POSTGRES_PORT=5432

DATABASE_URL=postgresql+psycopg://postgres:CHANGE_ME_STRONG_PASSWORD@db:5432/checking

FORMS_URL=https://forms.office.com/Pages/ResponsePage.aspx?id=QWJvW1ea5EuOUB36cueaV-4C0XpFTa1LmJM_FjZpp4pUOTFGR1QwSk00Vk5KQ0ExNUMzQldRSkpHWCQlQCN0PWcu&origin=QRCode
DEVICE_SHARED_KEY=CHANGE_ME_DEVICE_KEY
MOBILE_APP_SHARED_KEY=change-mobile-app-shared-key
PROVIDER_SHARED_KEY=PETROBRASP80P82P83
TRANSPORT_BOT_SHARED_KEY=change-transport-bot-shared-key
WHATSAPP_ENABLED=false
WHATSAPP_PROVIDER=meta
WHATSAPP_WEBHOOK_VERIFY_TOKEN=change-whatsapp-webhook-verify-token
WHATSAPP_ACCESS_TOKEN=change-whatsapp-access-token
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_APP_SECRET=
WHATSAPP_GRAPH_API_VERSION=v22.0
ADMIN_SESSION_SECRET=CHANGE_ME_RANDOM_SESSION_SECRET
BOOTSTRAP_ADMIN_KEY=HR70
BOOTSTRAP_ADMIN_NAME=Tamer Salmem
BOOTSTRAP_ADMIN_PASSWORD=CHANGE_ME_BOOTSTRAP_ADMIN_PASSWORD
TZ_NAME=Asia/Singapore
FORMS_TIMEOUT_SECONDS=30
FORMS_MAX_RETRIES=3
TRANSPORT_AI_ENABLED=false
TRANSPORT_AI_AGENT_MODE=agent
# Keep blank while Transport AI stays disabled in homologation/production.
# Only fill this after dedicated load validation and explicit operational approval.
TRANSPORT_AI_OPERATIONAL_APPROVAL_EVIDENCE=
# Keep at 1 unless a dedicated load test approves a higher ceiling.
TRANSPORT_AI_MAX_CONCURRENT_RUNS=1
TRANSPORT_AI_MAX_PASSENGERS_PER_RUN=80
TRANSPORT_AI_MAX_RUNTIME_SECONDS=180
# Required to open/save Transport AI settings. Generate once, keep stable across
# rollout/backfill, and do not rotate during a partial migration window:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TRANSPORT_AI_SETTINGS_ENCRYPTION_KEY=
MAPBOX_ACCESS_TOKEN=

# Project-scoped Transport AI provider keys are no longer configured in env.
# They are stored through the Transport UI or the controlled backfill script.
